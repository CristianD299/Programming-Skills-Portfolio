#include <iostream>
using namespace std;

// This program takes two integers as input and outputs their sum.
int main() {
    int a, b;
    cout << "Enter two numbers: ";
    cin >> a >> b; // Read two integers from user
    // Shows the sum of the two numbers
    cout << "Sum: " << a + b << endl;
    return 0;
}